# Memory Index

- [Use .venv/bin/pytest directly](feedback_venv_pytest.md) — Never chain `source .venv/bin/activate && pytest`; use `.venv/bin/pytest` (and `.venv/bin/<tool>` generally) to avoid permission prompts and honour the no-`&&` convention.
